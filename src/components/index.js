import Header from "./Header/Header";
import Footer from "./Footer/Footer";
import { HeroSlide, MovieList, MovieCard, MovieGrid } from "./components";
import { Button, OutlineButton, Modal, ModalContent, Input } from "./atoms";

export { Header, Footer, HeroSlide, MovieList, MovieGrid, MovieCard, Button, OutlineButton, Modal, ModalContent, Input };
